version: '5.3'
services:
  postgres:
    container_name: 'lingo_db'
    image: 'postgres:latest'
    env_file:
      - ./env/postgrest.env
    ports:
      - '1506:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: